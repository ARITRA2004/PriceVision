# Configure the BLIP-2 model for text extraction for image.

* Why Blip-2?
    * I use BLIP-2 because it converts raw images into meaningful, searchable text with good accuracy, without needing to train a vision model, and it works well as a first-stage product understanding model.
